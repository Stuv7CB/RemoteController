using Autofac;
using RemoteController.ViewModel;
using RemoteController.ViewModel.Abstractions;

namespace RemoteController
{
    public class DiModule : Module
    {
        protected override void Load(ContainerBuilder builder)
        {
            base.Load(builder);
        }
    }
}
